version: '3'
services:
    app:
        build: .
        volumes:
            - .:/app
            - /app/node_modules
        command: ['node', 'src/index.js']
    nginx:
        build:
            context: .
            dockerfile: Dockerfile.nginx
        ports:
            - '80:80'
        depends_on:
            - app
